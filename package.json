{
  "name": "redirect-servicee",
  "version": "1.0.0",
  "description": "Redirect service with bot detection",
  "main": "functions/_middleware.js",
  "scripts": {
    "dev": "wrangler pages dev",
    "deploy": "wrangler pages publish . --project-name=redirect-servicee"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
