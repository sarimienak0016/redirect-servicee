{
  "name": "shopee-affiliate-redirect",
  "version": "1.0.0",
  "description": "Shopee affiliate link redirect system",
  "main": "index.js",
  "scripts": {
    "dev": "wrangler pages dev public --compatibility-flags=nodejs_compat",
    "deploy": "wrangler pages deploy public --project-name=shopee-affiliate",
    "build": "echo 'No build step required'",
    "test": "echo 'No tests specified'"
  },
  "keywords": ["shopee", "affiliate", "redirect", "cloudflare"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
